package rst.automation;

option java_outer_classname = "RequestSlotsPosType";

/**
 * Requests positions of a list of slots (pre, pos each).
 */
message RequestSlotsPos {
	required string slotsList = 1;
}
